* Core: Make the layout table styleable (and style it).

* LayoutEditor: Add a way to initialize the canvas using a layout language.

* LayoutEditor: Add support for restoring the layout.

* Extensions: Make it possible to define attributes in the XML.

* Extensions: Add a width recommendation attribute to the XML.

* Extensions: Move the "recursive" attribute out of the code into the XML.

* Extensions: Add icon file attribute to the XML.

* Admin Center: Test better whether the permission escalation prevention
  works.

* Implement a Menu extension.

* Create a NavigationBar ("breadcrumbs") extension.

* Create an admin GUI for installing extensions.

* Implement a RecentChanges extension.

* Implement a MenuBar extension.


Long Term
----------
* Integrator/Extensions: Get the signal permission checks right.
